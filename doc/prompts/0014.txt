If the user is an admin, display a button the left navbar called "View Users"

If the user is an admin, clicking the "View Users" button should allow the admin to view a list of all users and manage user details.